room_selector = {
	default = "default_room" # points to textures that end with "_room.dds" but removes the file ending

	# will run with a limited country scope; species and government are set but the country does not actually exist
	game_setup = {
		"specialist_mercatorum_room" = { always = yes }
	}
}